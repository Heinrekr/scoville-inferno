info:
  name: get brands list
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/brands"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
